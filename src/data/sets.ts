// Every printed card set — shared by the Deck Builder (set filter), the
// admin Card Editor (a draft's own Set field), the Rarity Emblems tab (set
// is half of the set+rarity key), and the Text Layout tab's per-set
// copyright text overrides. Add a new set here once and it shows up
// everywhere at once, instead of updating four separate local copies.
export const SETS: string[] = ['Awakening'];
